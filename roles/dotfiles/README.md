# dotfiles

Installs personal dotfiles repo and supporting packages; optional starship prompt.

Variables (vars/main.yml)
- dotfiles_repo: git URL (SSH; requires controller access)
- dotfiles_dest: target clone path
- dotfiles_version: branch/tag
- starship: install starship prompt (default true)
