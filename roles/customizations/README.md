# customizations

Applies desktop tweaks: fonts, VS Code/Sublime install (optional), XFCE panel/browser settings.

Variables (defaults/main.yml)
- install_code: install VS Code (default false)
- install_sublime: install Sublime Text (default true)
- xfce4_customizations: apply XFCE tweaks (default true)
